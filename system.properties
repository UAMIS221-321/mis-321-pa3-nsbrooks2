# Heroku "system.properties" is normally used by the JVM buildpack.
# This .NET app pins the SDK via global.json and the runtime via Dockerfile (mcr.microsoft.com/dotnet/aspnet:9.0).
dotnet.sdk.version=9.0.304
dotnet.runtime.channel=9.0
